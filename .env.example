# Environment configuration for T4L End-to-End Pipeline

# Database (switch between SQLite and Supabase Postgres)
# SQLite (local dev):
DATABASE_URL=sqlite:///./t4l.db
# Postgres/Supabase (production):
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:PORT/DB

# Optional: Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# LLM / OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# App
LOG_LEVEL=INFO
